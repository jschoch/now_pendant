1.  add pot type input
2.  make the python just run the python in git so you only have to halcompile --install once
3.  get data back from lcnc server
    a.  need jog-scale
    b.  need some sort of status
    c.  position (G54?)